{
    "name": "khmarketing/kh-bounce",
    "description": "Exit intent modal plugin (development tooling)",
    "type": "wordpress-plugin",
    "require": {},
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "yoast/phpunit-polyfills": "^1.1"
    },
    "scripts": {
        "test": [
            "@phpunit"
        ],
        "phpunit": "WP_PHPUNIT__DIR=${WP_PHPUNIT__DIR:-/tmp/wordpress-tests-lib} vendor/bin/phpunit -c phpunit.xml.dist"
    },
    "config": {
        "sort-packages": true
    }
}
